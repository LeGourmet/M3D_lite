controller => 
	- implement gamepad => class fexible car depend du gamepad

renderer =>
	- implement un renderer opengl/vulkan/metal
		\=> init(windows)
		\=> drawFrame()
	- implement texture

scene =>
	- clear des texture et autres valeurs instancier pour l'engine
	- graphe de scene / découpage de la scene en tronçons pour rendu plus opti
	- ajout de lights (sun, autre, texture(env,lightsmap,...))
	- ajout controllers => mooves les elements de la scene/camera/lights 

mesh_triangle =>
	- stockage valeurs pour vertex/... pour engine
	- ajout d'autre textures / valeurs
	- utisier mieux loader / ajout autres loaders

user_interface =>
	- audio = > add set of sound + load sound + use sound + dispose sound => pas besoin de classe( fusionner avec window ?)
	- input manager = > windows_event agrandir / retrecir => pas besoin de classe (fusionner avec window ?)